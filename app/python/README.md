## interactive-presentation-app

Next-gen rewrite: **Python package** for parsing/compiling/export + **vanilla TS client** for rendering and interaction.

### Development

- **Web**:
  - `cd app/web && npm install && npm run dev`
- **Python**:
  - `cd app/python && python -m pip install -e .`
  - `interactive-presentation-app --help`

